use python 3.7.4  64 bit  

req -> 
pip install tensorflow transformers
pip install pandas 
pip install matplotlib
pip insatll seaborn 
pip install nltk
pip install ipykernel 



use maxlen = 1000 by default 
and if using BERT use maxlen as 512 (length of at max 512 can be tokenized)


try -> removing stop words reducing text length to smaller words and looking at key words 
